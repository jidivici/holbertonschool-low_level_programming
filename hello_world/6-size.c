#include <stdio.h>

/**
* main - entry point
*
* Return: 0 (sucess)
*/

int main(void)
{
	int i;
	float f;
	char c;

	printf("Size of char: %d bytes\n", (int)sizeof(c));
	printf("Size of int: %d bytes\n", (int)sizeof(i));
	printf("Size of int: %d bytes\n", (int)sizeof(i));
	printf("Size of int: %d bytes\n", (int)sizeof(i));
	printf("Size of float: %d bytes\n", (int)sizeof(f));
	return (0);
}
